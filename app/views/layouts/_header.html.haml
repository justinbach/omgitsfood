#header
  #logo
    %h1 
      %span#omg> omg 
      %span#its> its
      %span#food food
  #userStuff
    - if logged_in?
      ='you are ' + current_user.username
      |
      = link_to "Log out", logout_path
    - else
      = link_to "Sign up", signup_path
  #nav
    = link_to "plan meals", meals_path
    = link_to "manage recipes", recipes_path
    //= link_to "browse users", users_path
.clearDiv
  