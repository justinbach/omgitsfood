#header
  #logo
    %h1
      %a(href="/")
        %span#omg> omg 
        %span#its> its
        %span#food food
  #userStuff
    - if logged_in?
      ='you are ' + current_user.username
      |
      = link_to "Log out", logout_path
    - else
      = link_to "Sign up", signup_path
  - if current_user
    #nav
      = link_to "meals", user_meals_path(current_user)
      = link_to "recipes", recipes_path
      = link_to "users", users_path
.clearDiv
  