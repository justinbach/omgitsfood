.meal
  = button_to "x", { :controller => "meals", :action => "destroy", :id => meal }, :confirm => "Are you sure you wish to delete this meal?", :method => 'delete' if meal.user == current_user
  = link_to meal.recipe.title, user_meal_path(@user, meal)